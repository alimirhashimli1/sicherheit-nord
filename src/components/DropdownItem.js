import React from "react";

const DropdownItem = ({ text }) => {
  return (
        <a className="mb-2 no-underline text-black" href="">
        {text}
      </a>
  );
};

export default DropdownItem;
